export const metadata = {
  title: "Matchmaker",
  description: "Creator-brand matchmaking app",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
